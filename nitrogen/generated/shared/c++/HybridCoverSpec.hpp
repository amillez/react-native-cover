///
/// HybridCoverSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `CoverImageOptions` to properly resolve imports.
namespace margelo::nitro::cover { struct CoverImageOptions; }
// Forward declaration of `CoverBlurStyle` to properly resolve imports.
namespace margelo::nitro::cover { enum class CoverBlurStyle; }
// Forward declaration of `CoverEasing` to properly resolve imports.
namespace margelo::nitro::cover { enum class CoverEasing; }

#include <string>
#include "CoverImageOptions.hpp"
#include "CoverBlurStyle.hpp"
#include <optional>
#include "CoverEasing.hpp"

namespace margelo::nitro::cover {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Cover`
   * Inherit this class to create instances of `HybridCoverSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridCover: public HybridCoverSpec {
   * public:
   *   HybridCover(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridCoverSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridCoverSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridCoverSpec() override = default;

    public:
      // Properties
      virtual bool getIsEnabled() = 0;
      virtual bool getIsVisible() = 0;

    public:
      // Methods
      virtual void enable() = 0;
      virtual void disable() = 0;
      virtual void setColor(const std::string& hex) = 0;
      virtual void setImage(const CoverImageOptions& options) = 0;
      virtual void clearImage() = 0;
      virtual void setBlur(CoverBlurStyle style, std::optional<double> intensity) = 0;
      virtual void setFade(double durationMs, std::optional<CoverEasing> easing) = 0;
      virtual void show() = 0;
      virtual void hide() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Cover";
  };

} // namespace margelo::nitro::cover
